// svg.d.ts  (place at project root or in src/types and ensure tsconfig includes it)
declare module '*.svg' {
  import React from 'react';
  import { SvgProps } from 'react-native-svg';
  const content: React.FC<SvgProps>;
  export default content;
}
